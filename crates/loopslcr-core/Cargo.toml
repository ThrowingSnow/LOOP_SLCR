[package]
name = "loopslcr-core"
description = "Exact loop-trimming core: rational bar math, WAV I/O, cut, foldback"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
thiserror.workspace = true

[dev-dependencies]
# Reference implementation to check the hand-rolled reader against. Not a
# runtime dependency: hound cannot write acid/smpl chunks, which is why the
# reader and writer are our own. It earns its place the same way rubato will
# later -- as a trusted second opinion in tests.
hound = "3.5"
